<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        input, button{
            padding: 10px;
            outline: 1px solid gold;
            border: 2px solid green;
        }
    </style>
</head>
<body style="background: black; color:gold; min-height: 100vh; padding: 0; margin: 0; box-sizing: border-box; display: flex; align-items: center; justify-content: center;">
    <div class="" style="display: flex; justify-content: center; flex-direction: column; background: #003; max-width: 400px; width: 100%; padding: 10px; ">
        <h3 style="text-align: center;">Login =></h3>
        <form action="/login" method="post"  style="display: flex; flex-direction: column; gap: 1rem;">
            <label for="username">Username</label>
    
            <input type="text" name="username" id="username"> 
            <label for="password">Password</label>
            <input type="password" name="password" id="password">
       
            <button type="submit">Login</button>
        </form>
        <% if (error){ %>
            <p><%= error  %></p>
    
        <% } %>  
    </div>
   
</body>
</html>